/* 
  图片压缩处理
  使用规范   imgCompress(params).then(res=>{})
*/
/* 
  @actionName
  @params { File } img 图片文件流
  @params { Number } maxSize 压缩成功生成限制的最大大小 单位KB
  @params { Number } maxWidth 压缩成功生成的最大尺寸 
*/
export const imgCompress = (img: File, maxSize: number, maxWidth: number = 1000) => {};

/* 
  
*/
